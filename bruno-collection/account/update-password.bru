meta {
  name: Update Password
  type: http
  seq: 8
}

post {
  url: http://localhost:3000/account/password/update
  body: json
  auth: bearer
}

body:json {
  {
    "old-password": "currentpassword123",
    "new-password": "newpassword123",
    "confirm-password": "newpassword123"
  }
}